# Kappa sweep experiment: builds Pareto front over speed vs quality
# @package _global_

defaults:
  - default
  - _self_

experiment_name: "kappa_sweep"

# Run 02_solve_mdp.py once per kappa value, then 03_benchmark.py per policy
kappa_values: [0.0, 0.5, 1.0, 2.0, 5.0]
