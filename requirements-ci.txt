# CI-only dependencies with loose pins.
# torch is installed separately from the CPU wheel index in the workflow.
# Loose pins avoid breakage when a pinned version is yanked or unavailable for a Python release.

transformers>=4.40,<5
sentencepiece>=0.2
protobuf>=4
safetensors>=0.4
pytest>=8
scikit-learn>=1.4
pandas>=2
scipy>=1.13
pyarrow>=15
matplotlib>=3.8
hydra-core>=1.3
omegaconf>=2.3
datasets>=2.18
