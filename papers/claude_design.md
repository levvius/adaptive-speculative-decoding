# claude_design.md — бриф для Claude: визуальная презентация к защите ВКР

Этот файл — инструкция, **как и что описать Claude**, чтобы он собрал визуальную презентацию к
защите магистерской диссертации *JointAdaSpec*. Содержание слайдов и дословный текст доклада уже
есть в [`papers/pres.md`](pres.md) — здесь к ним добавлены дизайн-система, правила оформления и
**готовый мастер-промпт** (раздел 7), который достаточно скопировать в Claude.

---

## 1. Назначение и как пользоваться

1. Откройте новый чат с Claude (claude.ai), модель — Sonnet 4.6 или Opus.
2. **Прикрепите изображения** (раздел 5): нужные PNG из `reports/thesis_figs/png_slides/` и
   `papers/repo_qr.png`. Если прикрепить нельзя — Claude построит схемы сам и оставит для фигур
   подписанные плейсхолдеры.
3. Скопируйте **мастер-промпт из раздела 7** и при желании допишите 1–2 уточнения (вуз, ФИО
   руководителя, предпочитаемый формат вывода).
4. Получите деков, проверьте по чек-листу (раздел 9), при необходимости попросите Claude
   доработать отдельные слайды.

Источник истины по содержанию и числам — `pres.md` (там же канва речи на ~7 минут). Этот файл
задаёт **визуальную форму**, не переписывая содержание.

## 2. Контекст и аудитория

- **Тема.** Оптимизация больших языковых моделей методом адаптивного спекулятивного декодирования
  (метод JointAdaSpec на основе MDP).
- **Жанр.** Защита магистерской ВКР; аудитория — государственная экзаменационная комиссия.
- **Язык.** Русский (термины вводить как «рус. (english, АББР)» при первом упоминании).
- **Объём.** 12 слайдов, формат 16:9, регламент свободного доклада ~7 минут.
- **Структура (что важно).** Меньше теории, больше результатов: нет отдельного слайда-ликбеза по
  спекулятивному декодированию (его суть свёрнута в слайд актуальности), теория сжата в плашку на
  слайде метода, результатам отдано два слайда, оговорки сведены в один слайд ограничений.
- **Тон.** Академический, но визуально чистый и современный: минимум текста, крупные акценты,
  один смысловой посыл на слайд. Это научный доклад, а не маркетинговый питч.

## 3. Дизайн-система

**Палитра.**
- Заголовки / плашки: тёмно-индиго `#1B2A4A`.
- Фон: белый `#FFFFFF` (допустим очень светлый `#F7F8FA`).
- Основной текст: графит `#1F2430`.
- Акцент (выделить JointAdaSpec, активные элементы): бирюзовый `#2BB3A3`.
- **Семантические цвета (важно для честности):** зелёный `#1E9E5A` — только для статистически
  значимых положительных результатов (p < 0,05); нейтральный серый `#8A8F98` или приглушённый
  янтарь `#B5852A` — для честных оговорок, ничьих и нулевых результатов. Не красить нулевой
  результат красным как «провал» и значимый — ярко-зелёным как «победа над всеми».

**Типографика.**
- Заголовки: гротеск с кириллицей — Inter / Manrope / Montserrat, **Bold**, 36–40 pt.
- Текст и буллеты: тот же гротеск, Regular/Medium, 22–26 pt (минимум 24 pt для читаемости из зала).
- Код и формулы-фолбэк: моноширинный JetBrains Mono / Fira Code.
- Подписи к рисункам/таблицам: 16–18 pt, цвет `#5A6172`.
- Big-number callout: 60–96 pt (крупная цифра результата).

**Сетка и раскладки.** 12 колонок, поля ~6 % по краям, безопасная зона снизу под номер слайда и
(опц.) логотип вуза. Базовые мастер-раскладки:
1. **Титул** — центрированный текст.
2. **Проблема/секция** — крупный тезис сверху + 3–4 коротких буллета.
3. **Две колонки** — текст слева | рисунок справа (или наоборот).
4. **Big-number** — 1–2 крупные цифры + краткий контекст.
5. **Таблица результатов** — «зебра», выделить строку `jointadaspec` акцентом.
6. **Матрица/таксономия 3×3** — выделить пустую клетку.
7. **Формулы/карточки** — крупные формулы по центру или плитки-карточки.
8. **Закрывающий + QR** — крупный QR + URL.

**Графика.** Тонкие линейные иконки одного набора (например Lucide), без цветного клипарта и
фотостоков. Единый стиль рамок и теней (или вовсе без теней — плоско). Анимации минимальны:
только появление по клику, без «вращений».

## 4. Глобальные правила (честность — соблюдать строго)

Эти правила важнее красоты. Нарушение = искажение результатов работы.

- **Только зафиксированные числа** из `pres.md`. Ничего не выдумывать и не округлять «в свою
  пользу».
- **Никаких «быстрее всех / побеждает всех / SOTA / превосходит все методы».** Главный нарратив —
  «единый фреймворк + теория + честная характеризация границ».
- **Прямая генерация (AR) — самый быстрый метод.** «2,22×» указывать строго с подписью
  «относительно ванильного спекулятивного декодирования», не относительно AR.
- **joint ≈ cascade — статистическая ничья** (Δ = −0,13 %, p = 0,96). Не рисовать столбик joint
  выше cascade; на Парето они рядом.
- **Пара 7B/1.5B — нулевой результат** (3 независимых окна). Показывать честно, нейтральным
  цветом, как установленную границу применимости, а не прятать.
- **Немонотонность качества** по доле принятий (+7,4 / −2,6 п.п.) — показывать обе стороны.
- Формулы — через KaTeX/MathJax или вставлять как изображения LaTeX; **не** «сырой» юникод вида
  `∏_{t=1}^{n}`.
- Один смысловой посыл на слайд; на слайде — тезисы, развёрнуто проговаривается устно (текст в
  `pres.md`).

## 5. Инвентарь ассетов (какие изображения прикладывать и куда)

Готовые фигуры — в `reports/thesis_figs/png_slides/`. Рекомендованный набор для прикрепления
(нумерация слайдов — по новой 12-слайдовой структуре):

| Файл | Куда | Что показывает |
|---|---|---|
| `fig1_pareto.png` | Слайд 8 | Парето-фронт скорость↔качество; видно, что target_only — самый быстрый. |
| `fig_E_adaptivity_ablation.png` | Слайд 9 | JointAdaSpec vs фикс. порог fuzzy_sd: точность и скорость. |
| `fig_bonus_kappa_sweep.png` | Слайд 10 | κ-развёртка: joint ≈ cascade на всём диапазоне. |
| `fig_3win_robustness.png` | Слайд 10 (опц.) | Триангуляция 7B/1.5B по трём окнам (нуль). |
| `fig_G_acceptance_em.png` | Слайд 10 (опц.) | Немонотонность ΔEM по доле принятий. |
| `fig_policy_interpretability.png` | Слайд 6 | Интерпретируемость выученной политики. |
| `fig3_threshold_surface.png` | Слайд 6 (опц.) | Пороговая поверхность политики. |
| `fig_D_advantage_on_B.png` | Слайд 6 (опц.) | Преимущество каскада на множестве B ≈ 0 (пояснение к теореме D). |
| `papers/repo_qr.png` | Слайд 12 | QR на репозиторий. |

Прочие доступные: `fig2_paired_em.png`, `fig5_speedup.png`, `fig_0_headline.png` — резерв.

**Строить Claude самому (готовых PNG нет):** компактная схема цикла SD «черновик q → проверка p за
1 проход → принятый префикс» и две «ручки» γ, T (слайд 2), матрица-таксономия 3×3 (слайд 3),
крупные формулы s=(H,K,i) / a=(continue, verify@T) / r=η−c·t−κ·D (слайд 5), схема MDP «состояние →
политика-таблица → действие» (слайд 6), конвейер 01→04 (слайд 7).

> **Не использовать `fig4_conditions.png`** — в нём устаревшие числа массы нарушения μ*_J(B),
> противоречащие зафиксированным (μ*_J(B) ≈ 0,90); фигура исключена из финальной ВКР.
>
> **Честная пометка по подписям:** внутри готовых PNG подписи осей англоязычные (научные
> обозначения). Это допустимо для защиты; при желании можно перерисовать подписи на русский, но
> числа и кривые менять нельзя.

## 6. Послайдовая визуальная спецификация (12 слайдов)

Маппинг 1:1 на `pres.md`. Для каждого слайда: раскладка → заголовок → что на слайде → акцент.

1. **Титул** (раскладка 1). Тема; «Выпускная квалификационная работа (магистерская диссертация)»;
   Козин А.А.; научный руководитель — Калайдина Г.В.; КубГУ, ФКТиПМ, Центр искусственного
   интеллекта, кафедра анализа данных и искусственного интеллекта; Краснодар, 2026. Только текст,
   индиго на белом.
2. **Актуальность и проблема** (2 + big-number + мини-схема). Буллеты: инференс = 60–90 % стоимости;
   AR memory-bound; SD — стандарт (vLLM, TensorRT-LLM), где черновик предлагает блок, целевая
   проверяет за 1 проход; **γ и T фиксированы** (акцент). Big-number «60–90 %»; справа — компактная
   схема цикла SD с двумя «ручками» γ, T. Отдельного слайда-ликбеза по SD нет.
3. **Место среди методов и пробел** (6, матрица). Таксономия 3×3 (длина × верификация); **пустая
   клетка** «обучаемая длина × адаптивная верификация» = JointAdaSpec, выделить бирюзой.
4. **Цель и задачи** (2). Цель сверху крупно; ниже — 4 пронумерованные задачи.
5. **Обозначения и формулы** (7, формулы). Крупно по центру: s = (H, K, i); a = (continue |
   verify@T); r = η − c·t − κ·D; MDP ⟨S, A, P, r, λ⟩ и π\* из уравнения Беллмана (value iteration).
   Минимум текста — разворачивается устно. Формулы через KaTeX.
6. **Метод JointAdaSpec** (3, схема). Схема MDP: состояние → политика-таблица → действие
   continue/verify@T. Callout: |S| = 3600, |A| = 9, λ = 0,99, ≈ 2 КБ, поиск O(1); без обучения
   нейросетей; решается за секунды. Картинка `fig_policy_interpretability.png` (или
   `fig3_threshold_surface.png`). Компактная плашка «теоретические гарантии A/B/C/D/2.3/2.4»
   (теория сжата сюда, D выделить как ключевую).
7. **Реализация и воспроизводимость** (3). Конвейер 01→02→03→04 как поток; справа — аппаратура
   RTX 5090 (один GPU), Python 3.11+, PyTorch, scipy.sparse, Hydra, 94 pytest-теста на `v1-defense`,
   манифесты (git SHA, сиды [42,43,44], SHA-256), детерминизм; без переобучения базовых моделей.
8. **Результаты: главный прогон 14B/0.5B** (5 + 3). Таблица Run 1 (n = 1500) с выделенной строкой
   `jointadaspec`; явно подписать `artifact-backed thesis snapshot`. Рядом `fig1_pareto.png`.
   Big-number: **+4,07 п.п. EM**, p = 0,0205, **2,22×** (к ванильному SD), с подписью
   `fresh block-v1 claims live in main`. На Парето честно показать target_only как самую быструю точку.
9. **Результаты: адаптивность нетривиальна (эксперимент E)** (3). `fig_E_adaptivity_ablation.png`
   (две панели: точность и скорость). Callout: **+4…+8 п.п.** и **≈ 3,7×** против любого фикс.
   порога fuzzy_sd (14B/0.5B, n = 300); пометка `thesis-snapshot ablation evidence`.
10. **Ограничения и честная картина** (2 + рисунок). 4 буллета нейтральным/янтарным: joint ≈ cascade
    (ничья, p = 0,96, как предсказывает теорема D); AR — самый быстрый; 7B/1.5B — нуль (3 окна);
    немонотонность (+7,4 / −2,6 п.п.). Рисунок — `fig_bonus_kappa_sweep.png` или
    `fig_3win_robustness.png`. **Не** красить как провал.
11. **Заключение, вклад и положения на защиту** (2/карточки). Три вклада: единый MDP-фреймворк;
    теория A/B/C/D/2.3/2.4; честная характеризация границ. Рядом — 4 положения на защиту списком;
    ниже — направления развития (древовидное обобщение, распределительные признаки, GPU-резидентная
    политика).
12. **Спасибо + QR** (8). Крупный `papers/repo_qr.png` по центру/справа; URL
    `github.com/levvius/adaptive-speculative-decoding/tree/v1-defense`; «Спасибо за внимание».
    Держится во время вопросов.

## 7. Мастер-промпт (скопировать в Claude)

```text
Ты — дизайнер научных презентаций. Собери визуальную презентацию к защите магистерской
диссертации на русском языке. Формат 16:9, ровно 12 слайдов, регламент доклада ~7 минут.

ТЕМА: «Оптимизация больших языковых моделей методом адаптивного спекулятивного декодирования»
(метод JointAdaSpec на основе марковского процесса принятия решений). Автор — Козин А.А.

ФОРМАТ ВЫВОДА: один самодостаточный HTML-файл на reveal.js (изображения — как вложения, ссылайся
на них по именам). Если попрошу — пересобери в PPTX через python-pptx.

ДИЗАЙН-СИСТЕМА:
- Палитра: заголовки/плашки индиго #1B2A4A; фон белый; текст графит #1F2430; акцент бирюза #2BB3A3.
- Семантические цвета: зелёный #1E9E5A — только для значимых результатов (p<0,05); серый #8A8F98 /
  янтарь #B5852A — для честных оговорок и нулевых результатов.
- Шрифты: заголовки Inter/Manrope Bold 36–40pt; текст 22–26pt (минимум 24pt); моноширинный
  JetBrains Mono для кода и формул-фолбэка; подписи 16–18pt.
- Раскладки: титул; проблема (тезис + 3–4 буллета); две колонки текст|рисунок; big-number callout;
  таблица результатов (выдели строку jointadaspec); матрица 3×3; крупные формулы; закрывающий+QR.
- Иконки тонкие линейные, без клипарта. Один посыл на слайд, минимум текста.
- Формулы — через KaTeX (не сырой юникод).

ПРАВИЛА ЧЕСТНОСТИ (строго):
- Только числа, которые я даю ниже. Ничего не выдумывай.
- Запрещены формулировки «быстрее всех / побеждает всех / SOTA».
- Прямая генерация (target_only) — САМЫЙ БЫСТРЫЙ метод. «2,22×» — только относительно ванильного
  спекулятивного декодирования (подпиши это явно).
- joint ≈ cascade — статистическая НИЧЬЯ (Δ=−0,13%, p=0,96); не рисуй joint выше cascade.
- Пара 7B/1.5B — НУЛЕВОЙ результат; покажи честно, нейтральным цветом, как границу применимости.

СЛАЙДЫ (ровно 12):
1. Титул: тема; «магистерская диссертация»; Козин А.А.; научный руководитель — Калайдина Г.В.;
   КубГУ, ФКТиПМ, Центр искусственного интеллекта, кафедра анализа данных и ИИ; 2026.
2. Актуальность и проблема: инференс = 60–90% стоимости LLM; авторегрессия memory-bound; SD —
   стандарт (vLLM, TensorRT-LLM): черновик предлагает блок токенов, целевая модель проверяет за 1
   проход; ДВЕ «ручки» — длина γ и порог T — фиксированы и неадаптивны (акцент). Big-number «60–90%»
   + компактная схема цикла SD. (Отдельного слайда про SD не делай — суть здесь.)
3. Место среди методов и пробел: таксономия 3×3 (длина × верификация); пустая клетка «обучаемая
   длина × адаптивная верификация» = JointAdaSpec (выдели).
4. Цель и задачи: цель — совместное управление (длина, порог) как оптимальное управление; 4 задачи.
5. Обозначения и формулы (минимум текста, KaTeX): состояние s=(H,K,i); действие a=(continue |
   verify@T); награда r=η−c·t−κ·D; задача — MDP ⟨S,A,P,r,λ⟩, оптимальная политика π* из уравнения
   Беллмана. Крупные формулы по центру.
6. Метод: каждый шаг — действие в MDP; состояние → политика-таблица → действие continue/verify@T;
   |S|=3600, |A|=9, λ=0,99; политика — таблица ~2 КБ, поиск O(1), без обучения нейросетей, решается
   за секунды; политика интерпретируема. Добавь компактную плашку «теоретические гарантии»: A
   (выборочная сложность), B (исправленный reward shaping), C/D (оценка и ТОЧНЫЙ разрыв с каскадом),
   2.3/2.4 (слабое доминирование, Парето). Выдели D. (Построй схему «состояние → политика → действие».)
7. Реализация: конвейер 01 сбор трейсов → 02 решение MDP → 03 бенчмарк → 04 проверка условий;
   аппаратура RTX 5090 (один GPU); Python 3.11+, PyTorch, scipy.sparse, Hydra; 94 pytest-теста на
   `v1-defense`; манифесты (git SHA, сиды [42,43,44], SHA256), детерминизм; без переобучения моделей.
8. Результаты 14B/0.5B (GSM8K, n=1500, artifact-backed thesis snapshot). Таблица:
   target_only: EM 52,93% | 14,45 ток/с | — ;
   speculative: EM 53,27% | 4,88 ток/с | +0,33 п.п. | p=0,877 ;
   cascade: EM 57,13% | 10,29 ток/с | +4,20 п.п. | p=0,015 ;
   jointadaspec: EM 57,00% | 10,84 ток/с | +4,07 п.п. | p=0,020 (выдели строку).
   Big-number: +4,07 п.п. EM (p=0,0205); 2,22× к ванильному SD. Рисунок Парето — вложение
   fig1_pareto.png; на нём target_only — самая быстрая точка. Подпись: fresh block-v1 claims live in `main`.
9. Адаптивность нетривиальна (эксперимент E, 14B/0.5B, n=300, thesis-snapshot ablation):
   adaptive control точнее любого фиксированного fuzzy_sd на +4…+8 п.п. EM и ≈3,7× быстрее.
   Рисунок — fig_E_adaptivity_ablation.png.
10. Ограничения и честная картина (нейтральные/янтарные акценты): joint≈cascade — ничья (Δ=−0,13%,
    p=0,96, как предсказывает теорема D); прямая генерация — самый быстрый метод; 7B/1.5B — нуль на
    3 независимых окнах; прирост качества немонотонен (+7,4 п.п. при умеренной доле принятий, −2,6
    при высокой). Рисунок — fig_bonus_kappa_sweep.png.
11. Заключение, вклад и положения на защиту: вклады — (1) единый MDP-фреймворк совместного
    управления, одна из первых табличных постановок в литературе; (2) теория A/B/C/D/2.3/2.4;
    (3) честная характеризация границ. Положения: 1) совместное управление формализуемо как конечный
    MDP (~3600 состояний), решаемый точно; 2) доказаны его свойства; 3) реализован block-v1 pipeline
    с семантической валидацией артефактов; 4) зафиксированные прогоны показывают потенциал, границы
    применимости установлены. Направления: древовидное обобщение, распределительные признаки,
    GPU-резидентная политика.
12. Спасибо + QR: крупный QR (вложение repo_qr.png) на github.com/levvius/adaptive-speculative-
    decoding/tree/v1-defense; «Спасибо за внимание. Готов ответить на вопросы.»

Сначала покажи план слайдов одним сообщением, дождись моего «ок», затем собери HTML.
```

## 8. Форматы вывода и рекомендация

- **reveal.js (один HTML) — рекомендуется.** Самодостаточно, открывается в любом браузере и с
  проектора, легко показывать со своего ноутбука; картинки можно встроить base64. Минус — правки
  руками в HTML.
- **PPTX через python-pptx.** Удобно, если на защите требуют файл PowerPoint и нужно править в
  привычном редакторе. Минус — сложнее держать единый стиль и формулы.
- **Google Slides.** Совместная правка и показ из браузера; перенос дизайна — вручную по этому
  брифу.

**Про `papers/dist/` и `make slides`.** В `papers/dist/` лежит автособираемый деков
(`scripts/build_slides.sh` / `papers/build_slides.py`) — это **legacy-версия на 14 слайдов** прежней
структуры. Актуальная структура защиты — **12 слайдов этого брифа** и `pres.md`; авторитетный
визуальный деков собирается по данному брифу через Claude/reveal.js (раздел 7). Числа в обоих
случаях — только зафиксированные (см. §4).

## 9. Чек-лист перед защитой

- [ ] 12 слайдов; на каждом — один смысловой посыл, шрифт ≥ 24 pt.
- [ ] Все числа сверены с `pres.md`; `+4,07 / p = 0,0205 / 2,22× / +4…+8 п.п.`
      стоят рядом с пометкой `artifact-backed thesis snapshot / fresh claims in main`.
- [ ] Отдельного слайда-ликбеза по спекулятивному декодированию нет; его суть — на слайде 2.
- [ ] Честные оговорки сведены на слайд 10 и **не** поданы как провал/победа.
- [ ] «2,22×» подписано «к ванильному SD»; target_only обозначен как самый быстрый.
- [ ] Формулы — KaTeX/изображения, не юникод.
- [ ] На ключевых слайдах (3, 6, 8, 9, 10, 12) есть картинки/схемы.
- [ ] QR (слайд 12) сканируется и ведёт на репозиторий.
- [ ] `fig4_conditions.png` не использован.
- [ ] Прогон доклада укладывается в ~7 минут свободного рассказа (канва — в `pres.md`).
